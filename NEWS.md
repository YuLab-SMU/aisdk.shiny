# aisdk.shiny 0.1.0

* First release. Adds a Shiny-based web chat interface and configuration
  UI for the `aisdk` toolkit: ready-to-use Shiny modules and a server
  runtime for interactive, streaming conversations, including a model
  configuration panel and an optional `shinychat` backend.
